import React from 'react';

const ChatInfoOptions = ({ chatId }) => {
  return (
    <div className='chat-info__options'>
      <button className='red width100'>Delete Chat</button>
    </div>
  );
};

export default ChatInfoOptions;
